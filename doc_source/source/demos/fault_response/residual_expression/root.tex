%% Compile with: pdflatex -synctex=1 -interaction=nonstopmode root.tex

\documentclass[preview]{standalone}

%% Package includes
\usepackage{amsmath} % assumes amsmath package installed
\usepackage{amssymb}  % assumes amsmath package installed
\usepackage{mathtools} % Used to import the define sybmol
\usepackage{IEEEtrantools}
\usepackage{siunitx}
\usepackage{graphicx}
\usepackage{epstopdf}

\begin{document}
	\begin{equation*}
	r  \dot{V_a}(sin\beta-sin(\beta+f_\beta)) + V_a(cos\beta \dot{\beta} - cos(\beta+f_\beta)(\dot{\beta}+\dot{f_\beta})
	\end{equation*}
\end{document}
